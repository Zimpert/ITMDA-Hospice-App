<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.PartialViews.ContactItemView">
    <Frame CornerRadius="10" Padding="10" BackgroundColor="White" HasShadow="True" Margin="0,10">
        <Grid ColumnDefinitions="Auto, *, Auto" RowDefinitions="Auto">
            <!-- Avatar -->
            <Image Source="placeholder.png" HeightRequest="50" WidthRequest="50"
                   VerticalOptions="Center" Margin="10,0,20,0" />

            <!-- Contact Information -->
            <StackLayout Grid.Column="1" VerticalOptions="Center">
                <Label x:Name="NameLabel" FontAttributes="Bold" FontSize="Medium" TextColor="Black" />
                <Label x:Name="DetailsLabel" FontSize="Small" TextColor="Gray" />
            </StackLayout>

            <!-- Details Button -->
            <Button Text="Request Contact Removal" BackgroundColor="Red" TextColor="White" CornerRadius="10"
                    FontSize="Small" HeightRequest="40" 
                    VerticalOptions="Center" Grid.Column="2" />
        </Grid>
    </Frame>
</ContentView>
