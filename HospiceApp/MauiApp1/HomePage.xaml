<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.HomePage"
             Title="HomePage">
    
    <Grid Padding="20" RowSpacing="20" ColumnSpacing="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Top section with Profile Icon and Settings -->
        <StackLayout Orientation="Horizontal" Grid.Row="0" HorizontalOptions="Center">
            <Image Source="profile_icon.png" WidthRequest="30" HeightRequest="30"/>
            <Label Text="Home" FontAttributes="Bold" VerticalOptions="Center"/>
            <Image Source="settings_icon.png" WidthRequest="30" HeightRequest="30" HorizontalOptions="EndAndExpand"/>
        </StackLayout>

        <!-- Button Grid -->
        <Grid Grid.Row="1" RowSpacing="15" ColumnSpacing="15">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Navigation Buttons -->
            <Button Grid.Row="0" Grid.Column="0" Text="View Carer" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnViewCarerClicked"/>
            <Button Grid.Row="0" Grid.Column="1" Text="Shift Page" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnShiftPageClicked"/>
            <Button Grid.Row="1" Grid.Column="0" Text="Notification Page" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnNotificationPageClicked"/>
            <Button Grid.Row="1" Grid.Column="1" Text="QR Scan Page" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnQRScanPageClicked"/>
            <Button Grid.Row="2" Grid.Column="0" Text="Medication List" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnMedicationListClicked"/>
            <Button Grid.Row="2" Grid.Column="1" Text="Medication Tracker" BackgroundColor="#9b59b6" TextColor="White" Clicked="OnMedicationTrackerClicked"/>
            
        </Grid>
    </Grid>

</ContentPage>